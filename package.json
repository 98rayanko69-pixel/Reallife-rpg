{
  "name": "life-rpg",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p www && esbuild src/app.js --bundle --outfile=www/app.js && cp src/index.html www/index.html"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2",
    "@capacitor/local-notifications": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "esbuild": "^0.21.5"
  }
}
